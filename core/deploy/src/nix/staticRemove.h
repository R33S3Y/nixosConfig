#pragma once

#include <map>
#include <string>
#include <vector>
using namespace std;

class staticRemove {
public:
  static string comments(string fileStr);
  static string letIn(string fileStr);
};
